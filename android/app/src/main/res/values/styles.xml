<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <!-- The app UI is light-only; pin the window background so a device in dark
             mode doesn't flash a dark frame before React Native mounts. -->
        <item name="android:windowBackground">@color/splash_background</item>
    </style>

    <!-- Launch theme: shown by the system while the process starts. MainActivity
         swaps back to AppTheme in onCreate. -->
    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

</resources>
